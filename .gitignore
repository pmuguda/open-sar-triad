__pycache__/
*.pyc
.DS_Store
node_modules/
package.json
package-lock.json
.claude/
vendor/
# Impeccable design-tooling runtime state (pid/port/token for its live server).
# Critique snapshots under .impeccable/critique/ are intentionally NOT ignored.
.impeccable/live/

index-offline.html
index-record.html
data/scenes-small.geojson
js/app-record.js
node_modules/
package.json
package-lock.json

# Static API output. Derived from data/scenes.geojson by scripts/build_api.py and
# built into the Pages artifact at deploy time, so it reaches the CDN without ever
# entering git history (~87 MB, and it would be rewritten on every weekly refresh).
/api/

# Python client build artifacts
python/build/
python/dist/
python/*.egg-info/
.ipynb_checkpoints/
